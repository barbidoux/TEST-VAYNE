import { logInfo, logError } from '../../utils/logging'

test('logs info', () => {
  logInfo('Test info')

  // Add assertions for info logging
})

test('logs error', () => {
  logError('Test error')

  // Add assertions for error logging
})
